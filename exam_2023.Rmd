---
title: "Techniques de Programmation (6 ETCS)"
author: "Pierre Pelletier"
date: "3/6/2023"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

```

```{python setup, include=FALSE}

```


# Exercice 1 (6pts)

### Créer un objet 'X' vide. (vecteur, liste, etc.)  (1 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Ajouter progressivement des valeurs de 1 à 100 à cet objet en utilisant une boucle. (1 pts)


- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Créer un nouvel objet qui ne contiendra que les valeurs du premier objet qui sont divisibles par 7.(1 pts)


- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Réaliser les deux opérations précédentes sans utiliser de boucle en vectorisant l'opération. (1 pts)


- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Quels sont les avantages et les inconvénients liés à la vectorisation? Comparer les temps d'éxécution de vos deux approches. (2 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


# Exercice 2 (6 pts)

### Ouvrir les bases de données 'telephone.csv' et 'email.csv'. (0.5 pts)


- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


### Combien y a-t-il de personnes différentes dans 'telephone.csv' (variable ID) ? (0.5 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Expliquer le concept de jointure (merge/join), quels sont les différents types de jointure et quels sont leurs effets ? (1 pts)

left right inner outer full

### Joindre les deux bases de données en gardant uniquement les individus présents dans ces deux bases. Combien de personnes reste-t-il ? (1 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


### Combien y a-t-il de personnes ayant une adresse e-mail hébergée chez pvmail (xxx@pvmail.com) ? (1 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Quel est l'âge moyen des personnes pour chaque hébergement ? (2 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


# Exercice 3 (6 pts)

###  Créer un objet prenant la value '06' ou '07' aléatoirement. (0.5 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

###  Créer un objet prenant 8 valeurs comprise entre 0 et 9 et en former une chaine de charactère (string) (0.5 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

###  En utilisant vos réponses des deux dernières questions, créer maintenant un objet contenant 1000 numéros de téléphone générés aléatoirement, le numéro commencera par 06 ou 07 et contiendra ensuite huit chiffres allant de 0 à 9, que vous générerez selon une méthode de votre choix. (1 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Combien y a-t-il de numéros de téléphone se terminant par 7 ? (1 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Combien y a-t-il de numéros de téléphone contenant trois 7 placés à n'importe quel endroit ? (1 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


### En déduire une fonction capable de trouver tous les numéros contenant au moins n fois le chiffre x. (2 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


# Exercice 4 (6 pts)

###  Ouvrir la base de données 'df.csv'.Créer une variable 'Y' égale à la variable 'taille' multiplié par 2, ajouter à ce résultat la variable 'err' (y = 2*taille + err). (1.5 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Quelle est la variance de Y ? Faire un graphique de la distribution de Y. (1.5 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Créer une fonction qui réalise une régression linéaire sur n'importe quelle base de données comportant les variables 'Y' et 'taille', et qui renvoie le coefficient associé à la variable 'taille'. (2 pts)

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


###  Imaginons que l'on ai 100 jeu de donnée sur notre ordinateur ou l'on souhaite réaliser l'opération précédante, peut-on paralléliser cette tache? Expliquez. (1 pts)

# Exercice 5 (3pts)

### Importer le fichier 'reseau.csv'.(0.5 pts) Chaque ligne i correspond au lien de l'individu i avec les individus du reseau. Cette matrice est donc un réseau, et est symétrique. Si l'intersection entre l'individu i et j est un 1 alors ils ont un lien (0 = pas de lien). La diagonal est 0 car l'individu i n'a pas de lien avec lui même.

- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


### Quelle est la personne ayant le plus de liens avec d'autres personnes ? (1 pts)


- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```


### Qui connaît au moins deux personnes ? (0.5 pts)


- R

```{r, error = T, message = F}

```

- Python

```{python, error = T, message = F}

```

### Y a-t-il des manières plus efficaces pour stocker des matrices ayant beaucoup de 0 ? Expliquer (1 pts)




